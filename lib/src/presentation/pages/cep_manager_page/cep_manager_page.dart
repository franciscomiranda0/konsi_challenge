import 'package:flutter/material.dart';

class CepManagerPage extends StatelessWidget {
  const CepManagerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
